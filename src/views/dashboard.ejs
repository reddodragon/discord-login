<%- include('partials/header')%>

<div class="bg-dark">
    <div class="container text-light">
        <h1>Dashboard</h1>

        <p><%= user.username %>, thsese are your servers</p>

        <div class="row">
            <% user.guilds.forEach((guild) => { %>
                <%- include('partials/guild', { guild }) %>
            <% }) %>
        </div>
            </div>
        </div>

<%- include('partials/footer')%>
